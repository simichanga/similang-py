fn main() -> float {

    let name: str = "John";
    let age: int = (1 + 2*3 + 4) * 2;
    age += 2;

    let num: float = 1.2 + 2.3;

    printf("%s is %d years old.\n num is %f", name, age, num);

    return num - 2;
}
